name: Block Workflow Modifications Dispatch

on:
  workflow_run:
    workflows: ["Dispatch Restrict External PRs"]
    types:
      - completed

jobs:
  close-wf-mods-pr:
    uses: ./.github/workflows/block_wf_mods.yml
    with:
      ALLOWED_USERS: "KalebCoberly"
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}